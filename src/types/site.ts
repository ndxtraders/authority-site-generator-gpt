/** Site-wide types inferred from the executable runtime content contract. */
export type {
  Branding,
  Business,
  ConversionConfig,
  FooterConfig,
  GeoCoordinates,
  Navigation,
  NavigationLink,
  PostalAddress,
  SchemaConfig,
  SiteConfig,
} from "../lib/content-schema";
